阅读本文章您会收获到以下所有的收获
1.  使用新的创建返回结果
    code:0
    msg:"success"
    data:T
2.  使用AOP进行before和afterReturn的日志打印
3.  使用异常处理机制,可以只对于Controller层的异常进行处理采用注解@ControllerAdvice这个注解
4.  使用的一个yml的文件格式方式进行配置文件
5.  使用SpringBoot中的Controller中的(Student　student)
6.  尝试使用了GetMapping/PostMappint/DeleteMapping/PutMapping等注解
7.  使用了Spring4+的使用了RestfController注解来进行返回数据的json格式化
8.  使用了Util类进行重复代码的处理机制
9.  使用了枚举类来进行某些信息异常的处理
10. 使用了一种hibernate的方法,继承了某个类.然后使用不写sql就可以啦.
11. 使用了Controller中的占位符进行使用
     请求的url:/hello/12
     @GetMapping(value = "/hello/{a}")
     @PathVariable("a") String b
     logger.info(b)---->12
12. 使用了yml中的对象的方式给配置文件赋值



14.git fetch origin bug:bug








